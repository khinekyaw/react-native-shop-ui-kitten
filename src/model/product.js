export class Product {
  constructor(id, name, price, cat, imageUrl, description) {
    this.id = id
    this.name = name
    this.price = price
    this.cat = cat
    this.imageUrl = imageUrl
    this.description = description
  }
}
